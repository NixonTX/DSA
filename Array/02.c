// Problems using 1D Array
// Largest
#include<stdio.h>
int main() {
    int arr[5] = {1, 2, -3, 4, 5};
    int arr2[5];
    int size = 5;
    int size2 = 5;




   /* int sum = 0;

    for(int i=0; i<size; i++) {
        sum += arr[i];
    }

    printf("Sum is %d", sum); */


    // for(int i=0; i<size; i++) {
    //     arr2[i] = arr[size -1 -i];
    // }

    // for(int i=0; i<size2; i++) {
    //     printf("%d ", arr2[i]);
    // } 

    /*for(int i=0; i<size; i++) {
        arr2[i] = arr[i];
        
    }*/


    // int max = arr[0];
    // int min = arr[0];
   /* for(int i=1; i < size; i++) {
        if(arr[i] > max) {
            max = arr[i];
        }
        if(arr[i] < min) {
            min = arr[i];
        }
    }

    printf("Min is %d\nMax is %d", min, max); */


    /* for(int i=1; i<size; i++) {
        if(arr[i] > max) {
            max = arr[i];
        }
    }
    printf("The max ele is %d", max); */

    // Remove Duplicates - collect unique elements after sorting and print

    

    return 0;
}